---
interface Props {
  links: Links[] | null;
}
interface Links {
  title: string;
  url: string;
}
const { links } = Astro.props;

const defaultLinks: Links[] = [
  { title: "Trips", url: "/trips" },
  { title: "About", url: "/about" },
  { title: "Get in touch", url: "/contact" },
];
import { Icon } from "astro-icon/components";
---

<header class="text-white container self-center py-5 font-semibold">
  <nav class="flex items-center justify-between">
    <div class="flex gap-2 items-center ">
      <Icon name="hamb" class="text-4xl" />
      <p>MENU</p>
    </div>
    <div class="flex items-center justify-between w-1/2">
    <p class="text-2xl -ml-20">
      Antoni
      <span class="text-quaternary -ml-2">Gaudí</span>
    </p>
    <div class="text-quinary  flex gap-2">
      <Icon name="world" class="text-2xl"/>
      <p>Carrer d'Olot, 5, 08024 Barcelona</p>
    </div></div>
  </nav>
</header>
